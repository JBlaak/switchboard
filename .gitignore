node_modules/
dist/
*.db
*.db-wal
*.db-shm
.DS_Store
.idea/
*.pem
.env
.cache/
dev-app-update.yml
# Anchored to the root: the build output. Unanchored, this would also match
# any nested directory called `app` — it silently swallowed src/renderer/app/.
/app/
